import React from "react";
import { Text, View } from "react-native";

export const LoginView = () => {
    const t = "";

    return (
        <View>
            <Text>Login</Text>
        </View>
    );
};
